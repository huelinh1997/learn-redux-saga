const styles = (theme) => ({
  form: {
    padding: theme.spacing(2, 4, 3),
  },
  textField: {
    width: "100%"
  },
  select: {
    display: "flex",
    flexwrap: "wrap"
  }
})

export default styles
