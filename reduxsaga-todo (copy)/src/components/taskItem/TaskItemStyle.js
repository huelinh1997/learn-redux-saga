const styles = () => ({
    title: {
        display: "flex",
        flexWrap: "nowrap",
        justifyContent: "space-between"
    },
    cardAction: {
        display: "flex",
        justifyContent: "flex-end"
    }
})

export default styles

